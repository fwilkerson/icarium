# HLint configuration.
#
# Policy: treat hlint hints as design feedback first. The default rule
# set is curated; before adding an ignore, exhaust the refactor option.
# This file should stay near-empty.
